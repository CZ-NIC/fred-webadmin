stages:
    - analysis

before_script:
    - pip list

css:
    image: cznic/stylelint_runner:latest
    stage: analysis
    script:
        - stylelint --version
        - NODE_PATH="/usr/local/lib/node_modules" make check-css